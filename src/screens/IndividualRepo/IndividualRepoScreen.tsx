import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const IndividualRepoScreen = () => {
  return (
    <SafeAreaView>
      <Text>Individual Repository Screen</Text>
    </SafeAreaView>
  );
};

export default IndividualRepoScreen;
