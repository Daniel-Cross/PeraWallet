export enum ROUTES {
  REPO_LIST = "RepoList",
  INDIVIDUAL_REPO = "IndividualRepo",
  FAVOURITES = "Favourites",
  TABS = "Tabs",
}

export enum REQUEST_STATUS {
  IDLE = "idle",
  PENDING = "pending",
  SUCCESS = "success",
  ERROR = "error",
}
